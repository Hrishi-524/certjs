/*
generate api keys show only once
show prefix ... for all other generated api keys
each api key has a sub page that shows the api key usage and rate-limits
deactivation and deletion of api keys
*/

import React from 'react'

function page() {
  return (
    <div>page</div>
  )
}

export default page