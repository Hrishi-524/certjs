{
    "name": "certjs",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build -w @certjs/db && npm run build -w @certjs/queue && npm run build -w @certjs/core",
        "dev:api": "npm run dev -w certjs-api",
        "dev:worker": "npm run dev -w worker"
    },
    "dependencies": {
        "bullmq": "^5.72.1",
        "drizzle-orm": "^0.45.1",
        "ioredis": "^5.10.1",
        "postgres": "^3.4.8"
    }
}
